const router = require('express').Router();
/**
 * Notification API
 * Manages notification using firebase
 *
 * @prefix - /api/notification
 */

router.post('/', async (req,res) => {
  
});


module.exports = router;
